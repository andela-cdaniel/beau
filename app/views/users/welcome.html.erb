<main>
  <section class="container">
    <div class="row">
      <div class="col s12 center-align">
        <%= link_to "Beau.", root_path, class: "logo" %>
        <div class="content">
          <h1>
            Simple messaging for everyone.
          </h1>
          <p>
            Beau is a very simple and minimal implementation of a real-time application built
            using event-machine and websockets with the rails framework. Want to see what it's
            all about?
          </p>
          <%= link_to raw("<i class='material-icons left'>add</i>Sign up"), register_path, class: "btn" %>
          <%= link_to raw("<i class='material-icons right'>lock_open</i>Login"), login_path, class: "btn" %>
        </div>
      </div>
    </div>
  </section>
  <div class="bg-vid">
    <%= video_tag "bg-vid.mp4?#{rand(1..900)}", autoplay: true, loop: true, muted: true, preload: "auto", class: "vid" %>
  </div>
</main>